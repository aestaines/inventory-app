import { createApp } from 'vue';
import App from './App.vue';

createApp(App).mount('#app');


// import axios from 'axios';

// axios.defaults.baseURL = 'http://localhost:8000/api'; // Update with your backend URL
// axios.defaults.withCredentials = true; // To handle cross-origin requests if needed

// export default axios;